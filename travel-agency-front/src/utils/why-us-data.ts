import {
  CreditCard,
  FileStack,
  UserRound,
  Clock,
  Hand,
  Briefcase,
  Check,
} from "lucide-react";

const blocksIcons = [Clock, CreditCard, UserRound, Hand, Briefcase, FileStack];

const footerIcon = Check;

export { blocksIcons, footerIcon };
